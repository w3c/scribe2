#!/bin/bash

# Test that a failed s/// remains in the minutes, while a successful
# one is removed.

trap 'rm $TMP1 $TMP2 $TMP3 $TMP4' 0
TMP1=`mktemp /tmp/topics-XXXXXX` || exit 1
TMP2=`mktemp /tmp/topics-XXXXXX` || exit 1
TMP3=`mktemp /tmp/topics-XXXXXX` || exit 1
TMP4=`mktemp /tmp/topics-XXXXXX` || exit 1

cat >$TMP1 <<EOF
<foo> Original text
<foo> Anchor text
<foo> s/Original/Changed/
<foo> s/Non-existent/Changed/
EOF

cat >$TMP4 <<EOF
<div id=meeting class=meeting>
<h2>Meeting minutes</h2>
<section><p class=summary>Changed text</p>
<p class=summary>Anchor text</p>
<p class=summary>s/Non-existent/Changed/</p>
</section>
</div>
EOF

perl scribe.perl $TMP1 >$TMP2 || exit 1

# Extract just the minutes
sed -n -e '/<div id=meeting/,/<\/div>/p' $TMP2 >$TMP3

diff -u $TMP3 $TMP4
